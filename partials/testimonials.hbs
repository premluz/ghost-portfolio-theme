

<div class="gradient-frame"
     data-gradient-frame
     data-gradient-mode="invert"

     {{!-- BASE WAVE — the 4-colour ramp's animated core. --}}
     data-gradient-wave-a="#562967"
     data-gradient-wave-b="#19141E"
     data-gradient-scale="0.9"
     data-gradient-speed="0.7"

     {{!-- SECOND WAVE — composited in the same shader (no extra canvas).
           A DIFFERENT scale is what makes the two cross instead of stack.
           Omit wave2-a/-b to reuse the base pair at partial opacity. --}}
     data-gradient-wave2-a="#562967"
     data-gradient-wave2-b="#19141E"
     data-gradient-wave2-opacity="0.75"
     data-gradient-wave2-scale="1.4"
     data-gradient-wave2-speed="0.45"
     data-gradient-wave2-center="0.52"
     data-gradient-wave2-width="0.64"

     {{!-- PARALLAX — viewport-heights of scroll. Opposite signs make the
           two layers slide past each other; both 0 skips the scroll read. --}}
     data-gradient-parallax="0.05"
     data-gradient-wave2-parallax="-0.09"

     {{!-- PALETTE-SHIFT TIMING — ramp length in viewport heights, and the
           band render resolution (see gradflow-background.js). --}}
     data-gradient-enter-span="0.5"
     data-gradient-exit-span="0.5"
     data-gradient-resolution-scale="0.35">



<section class="testimonials-section animated-scroll-sequence" data-profile-background="#ffffff" data-testimonials-layout="{{@custom.testimonials_layout}}" data-skip-reveal>
 <header class="testimonials-header" data-sequence-animate data-scroll-distance="500" data-sticky-header="prevent">
    <h2 class="brand serif">Valued for</h2>
    <!-- <p class="section-description">delivering without drama.</p>-->
  </header>




    {{!-- RIGHT: image (pinned) — theme-responsive --}}
   <!-- <div class="profile-image-wrapper" id="profile-image-wrapper">

      {{!-- Theme-responsive portrait image --}}
      <img
        src="/content/images/prem-front-dark.png"
        alt="{{@site.title}}"
        class="profile-image profile-image-main"
        id="profile-portrait"
        data-dark-src="/content/images/prem-front-dark.png"
        data-light-src="/content/images/prem-front-light.png"
        loading="eager" 
      >

      {{!-- Fallback: site cover image --}}
     {{#if @site.cover_image}}
        <img
          src="{{@site.cover_image}}"
          alt="{{@site.title}}"
          class="profile-image profile-image-fallback"
          loading="eager"
        >
      {{/if}}

      <script>
        (function(){
          var img = document.getElementById('profile-portrait');
          var theme = document.documentElement.getAttribute('data-theme') || 'dark';
          if (theme === 'light') {
            img.src = img.getAttribute('data-light-src');
          } else {
            img.src = img.getAttribute('data-dark-src');
          }

          // Listen for theme changes
          window.addEventListener('themechange', function(e) {
            if (e.detail?.theme === 'light') {
              img.src = img.getAttribute('data-light-src');
            } else {
              img.src = img.getAttribute('data-dark-src');
            }
          });
        })();
      </script>
   </div> {{!-- /.profile-image-wrapper --}}

-->
  <div class="testimonials-grid" data-testimonials-layout="{{@custom.testimonials_layout}}">
    <div class="testimonials-track">
      {{#get "posts" filter="tag:testimonial" limit="100" include="tags,authors,html"}}
        {{#foreach posts}}
          {{> testimonial-card}}
        {{/foreach}}
      {{/get}}
    </div>


    
  </div>
</section>

</div>