{{!--
  PROFILE SECTION
  Mirrored from the HERO SECTION partial.
  Custom fields (set in Ghost Admin › Design):
    @custom.profile_name        — top label
    @custom.profile_title       — main headline
    @custom.profile_description — supporting paragraph
    @custom.profile_bullet_1/2/3 — tag pills
    @custom.profile_image       — portrait / right-side image (falls back to site cover)
--}}
<div class="gradient-frame"
     data-gradient-frame
     data-gradient-mode="invert"
     {{!-- Outer (page-facing) edge fades to transparent instead of a flat
           --color-background-matched color — page-about.hbs has its own
           whole-page fixed gradflow background behind this section, so a
           solid outer edge would read as a hard seam against it. Matches
           posts-tabs-grid-lab.hbs/testimonials.hbs, which carry this for
           the same reason on the homepage. Inner edge (this section's own
           invert-mode palette) stays fully opaque either way — fadeOuter
           only touches the side facing the page. NOTE this also requires
           the frame's OWN background to be carved out of the band zones,
           or it just shows through the now-transparent band: that's the
           [data-gradient-fade-outer='true'] rule in gradient-frame.css. --}}
     data-gradient-fade-outer="false"

     {{!-- BASE WAVE — the 4-colour ramp's animated core. --}}
     data-gradient-wave-a="#562967"
     data-gradient-wave-b="#19141E"
     data-gradient-scale="0.9"
     data-gradient-speed="0.7"

     {{!-- SECOND WAVE — composited in the same shader (no extra canvas).
           A DIFFERENT scale is what makes the two cross instead of stack.
           Omit wave2-a/-b to reuse the base pair at partial opacity. --}}
     data-gradient-wave2-a="#562967"
     data-gradient-wave2-b="#19141E"
     data-gradient-wave2-opacity="0.75"
     data-gradient-wave2-scale="1.4"
     data-gradient-wave2-speed="0.45"
     data-gradient-wave2-center="0.52"
     data-gradient-wave2-width="0.64"

     {{!-- PARALLAX — viewport-heights of scroll. Opposite signs make the
           two layers slide past each other; both 0 skips the scroll read. --}}
     data-gradient-parallax="0.05"
     data-gradient-wave2-parallax="-0.09"

     {{!-- PALETTE-SHIFT TIMING — ramp length in viewport heights, and the
           band render resolution (see gradflow-background.js). --}}
     data-gradient-enter-span="0.5"
     data-gradient-exit-span="0.5"
     data-gradient-resolution-scale="0.35">

<section class="profile" id="about" data-section-id="profile" data-home-profile-width="{{@custom.home_profile_width}}" "  data-profile-background="{{@custom.home_profile_background}}">
      <div class="profile-bg">
    <div class="profile-container"  data-page-content-width="full">

      {{!-- LEFT: text content --}}
      <div class="profile-content">

        {{!-- Top label 
        <p class="profile-intro" id="profile-intro" data-animate="letter">
          {{#if @custom.profile_name}}{{@custom.profile_name}}{{else}}I'm Prem{{/if}}
        </p>--}}

        {{!-- Main headline --}}
        <h2 class="profile-headline serif" id="profile-headline" data-animate="word">
          {{#if @custom.profile_title}}{{@custom.profile_title}}{{else}}The design role is shifting.{{/if}}
        </h2>
        {{!-- Supporting description (each paragraph animates like testimonial cards) --}}
        <div class="profile-description" id="profile-description">

          <p class="profile-paragraph" data-card-reveal="default" style="margin-bottom: 24px;">
           AI has collapsed the distance between idea and working product.  What's left scarce is judgment. That's the model I work in.
          </p>
        </div>


        {{!-- Six items grid: how I work, in practice — revealed as a single
              slide-up-staggered group by initProfileItemsReveal()
              (card-scroll-reveal.js), not per-item data-card-reveal (that
              drove each item off its OWN individual IntersectionObserver
              crossing, with data-item-index only ever adding a fixed extra
              delay after that — not a coordinated stagger from one trigger). --}}
        <div class="profile-items" id="profile-items">
          <div class="profile-item">
            <h3 class="profile-item-header">Partner</h3>
            <p class="profile-item-description">With engineers, product and executives, in their language. Influence earned by work.</p>
          </div>
          <div class="profile-item">
            <h3 class="profile-item-header">Lead</h3>
            <p class="profile-item-description">Hands-on at one end; mentoring, vision and design ops at the other.</p>
          </div>
          <div class="profile-item">
            <h3 class="profile-item-header">Build</h3>
            <p class="profile-item-description">Close to code. Complex products and systems.</p>
          </div>
          <div class="profile-item">
            <h3 class="profile-item-header">Care</h3>
            <p class="profile-item-description">Judgment, empathy, craft, coherence. That part stays human.</p>
          </div>
          <div class="profile-item">
            <h3 class="profile-item-header">Remove</h3>
            <p class="profile-item-description">More than I add. Killing bolt-ons and scope creep before they ship.</p>
          </div>
          <div class="profile-item">
            <h3 class="profile-item-header">Deliver</h3>
            <p class="profile-item-description">Value from day one, across 0→1→growth.</p>
          </div>
        </div>
       
      </div>{{!-- /.profile-content --}}



    </div>{{!-- /.profile-container --}}

    </div>{{!-- /.profile-container --}}

</section>{{!-- /.profile --}}
</div>

{{!-- Animation handled by unified scroll-scrub-anim.js via initProfile() --}}
